#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int d;
        cin >> d;
        int ans;
        for (int i = 1 + d; i <= 10000; i++)
        {
            for (int j = i + d; j <= 10000; j++)
            {
                
            }
        }
    }
    return 0;
}